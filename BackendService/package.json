{
  "name": "tcdd-mobile-service",
  "version": "1.0.0",
  "description": "backend service for tcdd bot",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.1",
    "selenium-webdriver": "^4.19.0"
  }
}
